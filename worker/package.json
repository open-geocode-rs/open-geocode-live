{
  "name": "open-geocode-demo-worker",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker front door for the open-geocode demo (UI + /api proxy).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "vitest": "~3.2.0",
    "wrangler": "^4"
  }
}
